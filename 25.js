// replace all instances of characters in string

var string = 'replacing all instances of characters in string';
var splitString = string.split('a');
var result = splitString.join('A');
console.log(result);
